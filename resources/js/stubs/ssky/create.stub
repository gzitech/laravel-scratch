module.exports = {

    props: {
        old: {
            type: [Object, Array],
            required: true,
        },
        errors: {
            type: [Object, Array],
            required: true,
        },
    },

    data() {
        return {
            #camelStudlyName#CreateForm: new SSky.Form({
                #create.return.item#
            }),
        };
    },


    watch: {

    },

    created() {
        
        if (this.errors && (typeof this.errors === 'object')) {
            this.#camelStudlyName#CreateForm.setErrors(this.errors);
        }
    },


    methods: {

        validate#studlyName#CreateForm: function (event) {

            this.#camelStudlyName#CreateForm.startProcessing();
            
            if(!this.#camelStudlyName#CreateForm.validate({
                #create.validate.item#
            })) {
                event.preventDefault();
                this.#camelStudlyName#CreateForm.finishProcessing();
            }
        },
    },


    computed: {

    }
};